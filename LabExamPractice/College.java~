import java.util.TreeSet;
import java.util.Scanner;

public class College{
  
  private TreeSet<Athlete> myTree;
  
  public College(){
    myTree = new TreeSet<Athlete>(new AthleteComparator());
  }
  
  public void createDB(String fname){
    Scanner sc = new Scanner(fname);
    
    while(sc.hasNextLine()){
      /*String line = sc.nextLine();
      String firstName = "";
      String lastName = "";
      String height = "";
      String birth = "";
      String sport = "";*/
      System.out.println("here");
      /*try{
        if(line.substring(0,1).compareTo("name: ") == 0) System.out.println(line.substring(6));
      }
      catch(Exception e){
        System.err.println("here");
      }*/
    }
  }
  
  public static void main(String[] args){
    College test = new College();
    test.createDB("sampleData.txt");
  }
  
}