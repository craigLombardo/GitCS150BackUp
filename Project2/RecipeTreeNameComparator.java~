import java.util.Comparator;

public class RecipeTreeNameComparator implements Comparator<String>{

  public int compare(Recipe one, Recipe two){
   return one.getName().compareTo(two.getName());
  }
  
}